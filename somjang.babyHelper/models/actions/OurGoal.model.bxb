action (OurGoal) {
  description (__DESCRIPTION__)
  collect {
    input (food) {
      type (Food)
      min (Optional) max (One)
    }

    input (age) {
      type (Age)
      min (Optional) max(One)
    }
    input (ingredient) {
      type (Ingredient)
      min (Optional) max (One)
    }

    input (goal_flag) {
      type (Goal_flag)
      min (Required) max (One)
    }
  }
  output (Result)
}
