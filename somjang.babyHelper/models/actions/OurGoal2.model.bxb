action (OurGoal2) {
  description (__DESCRIPTION__)
  collect {
    input (human) {
      type (Human)
      min (Required) max (One)
    }

    input (age) {
      type (Age)
      min (Optional) max (One)
    }

    input(goal_flag){
      type (Goal_flag)
      min (Optional) max (One)
    }
  }
  output (Result)
}
